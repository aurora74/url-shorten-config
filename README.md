# url-shorten-config

Configuration repository placeholder for the URL shortener DevOps assignment.

This repository is intended for non-secret environment manifests and deployment
configuration only. Do not commit runtime secrets, kubeconfigs, private keys,
tokens, generated benchmark output, or local VM artifacts.
